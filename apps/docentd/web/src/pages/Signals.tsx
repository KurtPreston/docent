import { Layout } from "../components/Layout";

export function Signals() {
  return (
    <Layout mainClass="wrap">
      <div className="empty">Signals (porting in progress).</div>
    </Layout>
  );
}
