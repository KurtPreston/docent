import { Layout } from "../components/Layout";

export function Dashboard() {
  return (
    <Layout mainClass="board">
      <div className="empty">Dashboard (porting in progress).</div>
    </Layout>
  );
}
