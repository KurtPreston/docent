import { Layout } from "../components/Layout";

export function Collectors() {
  return (
    <Layout mainClass="wrap">
      <div className="empty">Collectors (porting in progress).</div>
    </Layout>
  );
}
